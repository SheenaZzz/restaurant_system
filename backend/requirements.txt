# 骨架期用下限约束让 pip 解析当前版本。
# Step 2 会改成锁定版本（pip-tools / uv.lock），保证生产可复现。
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
psycopg[binary]>=3.2
pydantic>=2.9
alembic>=1.14
argon2-cffi>=23.1
pyjwt>=2.9
# zoneinfo 读的是系统 tz 数据库，slim 镜像里不保证有。
# 装这个包让 Python 有自带的一份兜底 —— 少了它，营业日会在
# ZoneInfoNotFoundError 上直接炸，而不是悄悄算错。
tzdata>=2024.1
