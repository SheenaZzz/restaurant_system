# Lower bounds during the skeleton phase, letting pip resolve current versions.
# Step 2 pins them (pip-tools / uv.lock) so production is reproducible.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
psycopg[binary]>=3.2
pydantic>=2.9
alembic>=1.14
argon2-cffi>=23.1
pyjwt>=2.9
# zoneinfo reads the system tz database, which a slim image does not guarantee.
# This package gives Python its own copy -- without it the business day blows
# up on ZoneInfoNotFoundError instead of quietly computing the wrong answer.
tzdata>=2024.1
